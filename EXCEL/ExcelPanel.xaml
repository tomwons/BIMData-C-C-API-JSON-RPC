<UserControl x:Class="BIMData.ExcelPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:rg="clr-namespace:unvell.ReoGrid;assembly=unvell.ReoGrid"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="800">

    <Grid Background="#F3F3F3">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Menu Grid.Row="0" Background="White" Padding="5">
            <MenuItem Header="_Plik">
                <MenuItem Header="Wczytaj plik (.xlsx)" Click="LoadExcelButton_Click"/>
                <MenuItem Header="Zapisz jako Excel (.xlsx)" Click="SaveExcelButton_Click"/>
            </MenuItem>
            <MenuItem Header="_Dane">
                <MenuItem Header="Pobierz z bazy" Click="LoadFromDbButton_Click"/>
                <MenuItem Header="Sortuj..." Click="SortButton_Click"/>
            </MenuItem>
        </Menu>

        <rg:ReoGridControl Grid.Row="1" Name="MySpreadsheet" Margin="0"/>

        <Border Grid.Row="2" Background="White" BorderBrush="#D3D3D3" BorderThickness="0,1,0,0" Padding="10">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="Szybkie akcje:" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="SemiBold" Foreground="#555555"/>
                <Button Name="QuickSumButton" 
                        Content="∑ Suma" 
                        Width="90" 
                        Height="32" 
                        Background="#E1E1E1" 
                        BorderThickness="1"
                        BorderBrush="#CCCCCC"
                        Click="QuickSumButton_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>